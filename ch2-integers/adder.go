package ch2integers

// Add takes two integers and returns the sum of them.
func Add(x, y int) int { // <- (x int, y int) can be shorten to (x, y int) when arguments are of the same type.
	return x + y
}